package com.example.service;


/**
 * Decorator pattern allowing the wrapped service to be called directly or as a rest service over the wire, depending on the
 * implementation chosen.
 */
public interface ServiceAProxy extends ServiceA{
}
